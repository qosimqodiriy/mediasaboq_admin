export interface Social {
  id: number,
  url: string,
  priority: number
  icon: string,
}
export interface Form {
  id: number | null,
  url: string | null,
  priority: number | null,
  icon: string | null
}
